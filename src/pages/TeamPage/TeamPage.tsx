import styles from './TeamPage.module.css';

function TeamPage() {
  return (
    <div className={styles.page}>
      <h1 className={styles.title}>TEAM</h1>
      <p>Team page coming soon...</p>
      {/* will have member headshots, names, and roles ? okay*/}
    </div>
  );
}

export default TeamPage;
